import React, { useState } from "react";
import axios from "../axios-config";

const NoteList = () => {
  return (
    <div>
      <h1>This is NoteList</h1>
    </div>
  );
};

export default NoteList;
