import React from "react";
import { BrowserRouter as Router, Route, Switch } from "react-router-dom";

const Catalog = () => {
  return <div className="catalog-view"></div>;
};

export default Catalog;
